[project]
name = "fraud-detector"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["shared"]

[tool.uv.sources]
shared = { path = "../../shared" }

[dependency-groups]
dev = ["pytest>=8.3", "pytest-asyncio>=0.24"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["."]
